data/raw/case_study/*.npy filter=lfs diff=lfs merge=lfs -text
references/legacy_abaqus/*.py -text -diff
# LFS is intentionally opt-in for durable golden references only.  Existing
# legacy pointers under validation/reference_data remain readable, but new
# validation outputs must not acquire LFS tracking through a broad glob.
validation/golden/**/*.npy filter=lfs diff=lfs merge=lfs -text
validation/golden/**/*.npz filter=lfs diff=lfs merge=lfs -text
