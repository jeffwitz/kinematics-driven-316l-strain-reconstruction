# Invalidated measurement-chain execution

This execution used `finest_scale=1` and stopped above the native image
resolution. It is retained only for provenance.

It must not be used for:

- DIC spatial-resolution claims;
- band-width metrology;
- comparison with a micromorphic length;
- the documentation evidence registry.

The corrected native-scale execution is
`validation/reference_data/dic_measurement_chain_v2/`.
