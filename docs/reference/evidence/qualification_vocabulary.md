# Qualification vocabulary

**Mode:** reference  
**Domain:** evidence

Use `verified` only when a reproducible artefact and declared tolerance support
the claim. Use `supported` when evidence is consistent but a qualification
boundary remains. Use `falsified` for a registered test that rejects the
candidate. Use `unknown` when the available data do not discriminate the
hypotheses.
