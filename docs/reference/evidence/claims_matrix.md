# Claims matrix

**Mode:** reference  
**Domain:** evidence

The claims matrix is the stable index of what is verified, supported,
provisional, not demonstrated or deliberately not claimed. It does not replace
the primary report and it does not upgrade a status when a new implementation
is faster.

```{include} ../../_generated/claims_matrix.inc
```
