# Installation and build

**Mode:** how-to  
**Domain:** software

- {doc}`install`
- {doc}`build_mfront`

Install the Python environment first, then build the optional MFront reference
library when a qualified generic backend is required.

```{toctree}
:hidden:

install
build_mfront
```
