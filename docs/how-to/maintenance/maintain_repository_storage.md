# Maintain repository storage

**Mode:** how-to  
**Domain:** operations

Keep code, tests, manifests, reports and metrics in Git. Keep only rare golden
references in Git LFS and archive large campaigns externally with a SHA-256
manifest. Follow {doc}`../../reference/operations/repository_storage_policy`.
