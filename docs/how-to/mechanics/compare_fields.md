# Compare reconstructed fields

**Mode:** how-to  
**Domain:** evidence

Place prediction and reference fields on the same physical grid and mask, then
run the recorded comparison metrics. Report amplitudes and localisation
overlap separately; never use a hidden registration or filtering step. See
{doc}`../compare_fields` for the complete command interface and
{doc}`../../reference/evidence/validation_metrics` for definitions.
